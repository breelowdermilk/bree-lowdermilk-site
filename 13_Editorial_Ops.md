# Editorial Ops
- Author new songs by copying `/06_Sample_Content/songs/example.mdx`.
- Commit to `main` → CI builds → deploy (Cloudflare/Netlify).
- For non‑technical editing, optionally add Decap CMS later.
- Keep a `content-styleguide.md` for titles, tags, and slugs.
